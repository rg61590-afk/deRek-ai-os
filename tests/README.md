# tests

Root-level integration/cross-app tests live here as the project grows.

Backend unit tests currently live alongside the API app at
`apps/api/tests/` and run via `pytest` from `apps/api/`. This directory
is reserved for tests that span multiple apps/packages once those
exist.
