# packages/tasks

Reserved for the future task execution/orchestration layer (units of
work the Kernel can schedule and run — the building block agents and
other capabilities will queue work through).

Empty package structure only in v0.0.1 — no implementation yet.
