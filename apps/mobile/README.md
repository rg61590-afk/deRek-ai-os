# apps/mobile

Out of scope for v0.0.1.

The project foundation defined in this release covers the API
(`apps/api`) and the web dashboard (`apps/dashboard`) only. This
directory is reserved so the mobile client can be added in a future
release without changing the top-level repository layout.
