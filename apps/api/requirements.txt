# deRek AI OS — Backend Dependencies
#
# Minimum versions are set to the lowest releases known to support
# Python 3.14. The original pydantic 2.10.4 bundled pydantic-core
# 2.27.x, which used an older PyO3 that lacked Python 3.14 support,
# causing source builds to fail. pydantic 2.11.0+ depends on
# pydantic-core 2.29.0+, which ships Python 3.14 wheels and resolves
# that failure. FastAPI 0.116.0+ explicitly supports Python 3.14.
#
# Upper bounds on major-version boundaries prevent unexpected breaking
# changes. Patch-level security and compatibility updates are
# auto-eligible, consistent with the project policy of maintaining
# compatibility with the latest stable Python release.

fastapi>=0.116.0,<1.0.0
uvicorn[standard]>=0.34.0
pydantic>=2.11.0,<3.0.0
pydantic-settings>=2.7.1,<3.0.0
python-dotenv>=1.0.1
httpx>=0.28.1
pytest>=8.3.4
pytest-asyncio>=0.25.2
